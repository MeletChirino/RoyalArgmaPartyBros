# Royal Argma Party Bros

## Install repo
You must create a virtual environment:

```ssh
python3 -m venv ./venv
```

Then open it on linux

```ssh
source venv/bin/activate
```

but in windows 
```cmd
.\venv\Scripts\activate
```